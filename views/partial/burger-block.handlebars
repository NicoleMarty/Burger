<li>
    {{burger_name}}

    <button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
        {{#if devoured}}NOM NOM O'CLOCK!!{{else}}I'm stuffed!{{/if}}
    </button>
</li>